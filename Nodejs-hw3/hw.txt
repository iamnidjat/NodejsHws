let array = []

У вас будет пустой массив который каждый будет увеличиваться на одно имя при срабатывании обработчика события ‘request’. ‘request’ будет срабатывать четыре раза. При каждом увеличении массива на одно имя у вас срабатывает обработчик событий созданный вами который будет писать, что в массив записалось новое. Каждый раз после увеличения массив будет посылаться на сервер при ‘request’. Также все записанные имена должны выходить в консоль при срабатывании вашего собственного обработчика. В конце обработчики событий нужно будет удалить
